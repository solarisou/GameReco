python -m venv venv
venv\Scripts\activate * à faire pour api.py lancer Flask :) * 
pip install -r requirements.txt


USE ter_reco;
SELECT COUNT(*) FROM items WHERE type = 'manga';
SELECT COUNT(*) FROM genres;
SELECT title, avg_rating FROM items WHERE type = 'manga' ORDER BY avg_rating DESC LIMIT 5;



Pour Tensorflows (TSFR)
dans le (venv)

pip install tensorflow tensorflow-recommenders


Pour igdb_loader.py il faut charger le .env avec cette commande 
pip install python-dotenv --break-system-packages